#include <iostream>
#include <fstream>

using namespace std;

int main()
{
  ifstream adventText("../input.txt");

  if (adventText.is_open())
  {
    while(adventText.)
    int num;
    adventText >> num;
    cout << num;
  }
  return 0;
}